# 芒果测试平台 - 测试数据获取方法文档

本文档汇总了 `mangotools.data_processor` 模块中所有获取测试数据的方法。

---

## 目录

1. [RandomCharacterInfoData - 随机人物信息数据](#randomcharacterinfodata---随机人物信息数据)
2. [RandomNumberData - 随机数字类型数据](#randomnumberdata---随机数字类型数据)
3. [RandomStringData - 随机字符串类型数据](#randomstringdata---随机字符串类型数据)
4. [RandomTimeData - 随机时间类型数据](#randomtimedata---随机时间类型数据)
5. [JsonTool - JSON 处理工具](#jsontool---json-处理工具)
6. [CacheTool - 内存缓存工具](#cachetool---内存缓存工具)
7. [CodingTool - 编码工具](#codingtool---编码工具)
8. [EncryptionTool - 加密工具](#encryptiontool---加密工具)
9. [SqlCache - SQL 缓存工具](#sqlcache---sql-缓存工具)

---

## RandomCharacterInfoData - 随机人物信息数据

用于生成随机的人物相关信息，如姓名、手机号、身份证号等。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `character_phone()` | 随机生成手机号码 | 无 | `str` |
| `character_id_number()` | 随机生成身份证号码 | 无 | `str` |
| `character_female_name()` | 女生姓名 | 无 | `str` |
| `character_male_name()` | 男生姓名 | 无 | `str` |
| `character_simple_profile()` | 获取简单的人物信息 | 无 | `str` |
| `character_profile()` | 获取带公司的人物信息 | 无 | `str` |
| `character_email()` | 生成邮箱 | 无 | `str` |
| `character_bank_card()` | 银行卡 | 无 | `str` |
| `character_address()` | 带邮政编码的地址 | 无 | `str` |
| `character_job()` | 获取职称 | 无 | `str` |
| `character_company()` | 获取公司名称 | 无 | `str` |
| `str_company_job()` | 生成随机公司职位 | 无 | `str` |
| `str_job()` | 生成随机职业 | 无 | `str` |
| `str_university()` | 生成随机大学名 | 无 | `str` |
| `str_intl_phone()` | 生成随机国际电话号码字符串 | 无 | `str` |
| `str_license_plate()` | 生成随机车牌号 | 无 | `str` |
| `str_postcode()` | 生成随机邮政编码 | 无 | `str` |
| `str_company_email()` | 生成随机公司邮箱 | 无 | `str` |

---

## RandomNumberData - 随机数字类型数据

用于生成各种类型的随机数字数据。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `randint(left=1, right=1000)` | 随机的范围数 | `left`: 最小值(默认1), `right`: 最大值(默认1000) | `int` |
| `number_time_5()` | 获取基于当前时间戳的随机五位数 | 无 | `str` |
| `number_random_0_9()` | 0-9的随机数 | 无 | `int` |
| `number_random_10_99()` | 10-99的随机数 | 无 | `int` |
| `number_random_100_999()` | 100-999的随机数 | 无 | `int` |
| `number_random_0_5000()` | 0-5000的随机数 | 无 | `int` |
| `number_random_float()` | 小数 | 无 | `float` |
| `number_random_two_float()` | 随机两位小数 | 无 | `float` |
| `number_random_1000_two_float()` | 1000以内的随机两位小数 | 无 | `float` |
| `number_random_even(left=0, right=1000)` | 指定范围内的随机偶数 | `left`: 最小值(默认0), `right`: 最大值(默认1000) | `int` |
| `number_random_odd(left=0, right=1000)` | 指定范围内的随机奇数 | `left`: 最小值(默认0), `right`: 最大值(默认1000) | `int` |
| `number_random_positive_float(left=0.0, right=1000.0)` | 指定范围内的随机正浮点数 | `left`: 最小值(默认0.0), `right`: 最大值(默认1000.0) | `float` |
| `number_random_negative_float(left=-1000.0, right=-0.01)` | 指定范围内的随机负浮点数 | `left`: 最小值(默认-1000.0), `right`: 最大值(默认-0.01) | `float` |
| `number_random_scientific()` | 随机科学计数法数字（字符串） | 无 | `str` |
| `number_random_percent()` | 随机百分数（0-100，带两位小数） | 无 | `float` |
| `number_random_money(left=0.01, right=10000.00)` | 随机金额（两位小数） | `left`: 最小值(默认0.01), `right`: 最大值(默认10000.00) | `float` |
| `number_random_mobile()` | 随机手机号码（数字型，11位） | 无 | `int` |
| `number_random_code4()` | 4位数字验证码 | 无 | `int` |
| `number_random_code6()` | 6位数字验证码 | 无 | `int` |
| `number_random_prime(left=2, right=100)` | 指定范围内的随机质数 | `left`: 最小值(默认2), `right`: 最大值(默认100) | `int` |
| `number_random_negative_even(left=-1000, right=-2)` | 负数范围内的随机偶数 | `left`: 最小值(默认-1000), `right`: 最大值(默认-2) | `int` |
| `number_random_negative_odd(left=-999, right=-1)` | 负数范围内的随机奇数 | `left`: 最小值(默认-999), `right`: 最大值(默认-1) | `int` |
| `number_random_decimal(digits=3)` | 随机小数（指定小数位数） | `digits`: 小数位数(默认3) | `float` |
| `number_positive_infinity()` | 正无穷（float('inf')） | 无 | `float` |
| `number_negative_infinity()` | 负无穷（float('-inf')） | 无 | `float` |
| `number_nan()` | NaN（float('nan')） | 无 | `float` |
| `number_random_exponential()` | 随机指数型浮点数（如1.23e+10） | 无 | `float` |
| `number_random_fraction_str()` | 随机分数字符串（如'3/7'） | 无 | `str` |
| `number_random_roman()` | 随机罗马数字字符串（1-3999） | 无 | `str` |
| `number_random_bin_str(digits=8)` | 随机二进制字符串 | `digits`: 位数(默认8) | `str` |
| `number_random_oct_str(digits=6)` | 随机八进制字符串 | `digits`: 位数(默认6) | `str` |
| `number_random_hex_str(digits=6)` | 随机十六进制字符串 | `digits`: 位数(默认6) | `str` |

---

## RandomStringData - 随机字符串类型数据

用于生成各种类型的随机字符串数据。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `str_uuid()` | 随机的UUID，长度36 | 无 | `str` |
| `str_random_string(length=10)` | 随机字母数字 | `length`: 长度(默认10) | `str` |
| `str_city()` | 获取城市 | 无 | `str` |
| `str_country()` | 获取国家 | 无 | `str` |
| `str_province()` | 获取省份 | 无 | `str` |
| `str_pystr()` | 生成英文的字符串 | 无 | `str` |
| `str_word()` | 生成词语 | 无 | `str` |
| `str_text()` | 生成一篇文章 | 无 | `str` |
| `str_lowercase(length=10)` | 生成指定长度的纯小写字母字符串 | `length`: 长度(默认10) | `str` |
| `str_uppercase(length=10)` | 生成指定长度的纯大写字母字符串 | `length`: 长度(默认10) | `str` |
| `str_special_chars(length=10)` | 生成指定长度的特殊字符字符串 | `length`: 长度(默认10) | `str` |
| `str_url()` | 生成随机URL | 无 | `str` |
| `str_ipv4()` | 生成随机IPv4地址 | 无 | `str` |
| `str_mac_address()` | 生成随机MAC地址 | 无 | `str` |
| `str_ipv6()` | 生成随机IPv6地址 | 无 | `str` |
| `str_date()` | 生成随机日期字符串（YYYY-MM-DD） | 无 | `str` |
| `str_time()` | 生成随机时间字符串（HH:MM:SS） | 无 | `str` |
| `str_datetime()` | 生成随机日期时间字符串（YYYY-MM-DD HH:MM:SS） | 无 | `str` |
| `str_url_path()` | 生成随机URL路径 | 无 | `str` |
| `str_hex_color()` | 生成随机HEX颜色 | 无 | `str` |
| `str_uuid_no_dash()` | 生成无短横线的UUID字符串 | 无 | `str` |
| `str_en_sentence()` | 生成随机英文句子 | 无 | `str` |
| `str_en_paragraph()` | 生成随机英文段落 | 无 | `str` |
| `str_cn_address()` | 生成随机中文地址 | 无 | `str` |
| `str_domain()` | 生成随机域名 | 无 | `str` |
| `str_filename(extension='txt')` | 生成随机文件名，可指定扩展名 | `extension`: 扩展名(默认txt) | `str` |
| `str_file_path()` | 生成随机文件路径 | 无 | `str` |
| `str_image_url()` | 生成随机图片URL | 无 | `str` |
| `str_latlng()` | 生成随机经纬度字符串 | 无 | `str` |
| `str_json()` | 生成随机JSON字符串 | 无 | `str` |
| `str_base64(length=10)` | 生成随机Base64字符串 | `length`: 长度(默认10) | `str` |
| `str_emoji()` | 生成随机Emoji表情 | 无 | `str` |
| `str_color_name()` | 生成随机颜色名 | 无 | `str` |
| `str_markdown()` | 生成随机Markdown文本 | 无 | `str` |
| `str_sql()` | 生成随机SQL语句字符串 | 无 | `str` |
| `str_url_params(count=3)` | 生成随机URL参数字符串 | `count`: 参数数量(默认3) | `str` |
| `str_card_number_split()` | 生成带空格分隔的银行卡号字符串 | 无 | `str` |
| `str_isbn()` | 生成随机ISBN号字符串 | 无 | `str` |
| `str_vin()` | 生成随机车架号（VIN）字符串 | 无 | `str` |
| `str_wechat_id()` | 生成随机微信号字符串 | 无 | `str` |
| `str_qq()` | 生成随机QQ号字符串 | 无 | `str` |
| `str_short_url()` | 生成随机短链字符串 | 无 | `str` |
| `str_special_mix(length=10)` | 生成随机特殊符号混合字符串 | `length`: 长度(默认10) | `str` |
| `str_whitespace(length=10)` | 生成大段空白/制表符/换行符字符串 | `length`: 长度(默认10) | `str` |
| `str_url_encoded(length=10)` | 生成随机URL编码字符串 | `length`: 长度(默认10) | `str` |
| `str_json_nested()` | 生成随机嵌套JSON字符串 | 无 | `str` |
| `str_html_escape()` | 生成随机HTML转义字符串 | 无 | `str` |
| `str_with_emoji()` | 生成带emoji的随机文本字符串 | 无 | `str` |

---

## RandomTimeData - 随机时间类型数据

用于生成各种格式的时间相关数据。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `time_now_ymdhms(minute=0)` | 当前年月日时分秒 | `minute`: 分钟偏移(默认0) | `str` |
| `time_random_utcnow()` | 当前UTC时间字符串（YYYY-MM-DD HH:MM:SSZ） | 无 | `str` |
| `time_now_ymd(days=0)` | 当前年月日 | `days`: 天数偏移(默认0) | `str` |
| `time_now_hms(minutes=0)` | 当前时分秒 | `minutes`: 分钟偏移(默认0) | `str` |
| `time_now_ymd_h(hours=0)` | 当前年月日时 | `hours`: 小时偏移(默认0) | `str` |
| `time_now_ymd_hm(minutes=0)` | 当前年月日时分 | `minutes`: 分钟偏移(默认0) | `str` |
| `time_stamp(minute=0)` | 几分钟后的时间戳 | `minute`: 分钟偏移(默认0) | `int` |
| `time_next_minute(minute=0)` | 几分钟后的年月日时分秒 | `minute`: 分钟偏移(默认0) | `str` |
| `time_now_year(days=0)` | 获取当前年份 | `days`: 天数偏移(默认0) | `str` |
| `time_now_month(days=0)` | 获取当前月份 | `days`: 天数偏移(默认0) | `str` |
| `time_now_day(days=0)` | 获取当前日期 | `days`: 天数偏移(默认0) | `str` |
| `time_now_hour(hours=0)` | 获取当前小时 | `hours`: 小时偏移(默认0) | `str` |
| `time_now_minute(minutes=0)` | 获取当前分钟 | `minutes`: 分钟偏移(默认0) | `str` |
| `time_now_second(seconds=0)` | 获取当前秒 | `seconds`: 秒偏移(默认0) | `str` |
| `time_random_year()` | 获取随机年份 | 无 | `str` |
| `time_random_month()` | 获取随机月份 | 无 | `str` |
| `time_random_date()` | 获取随机日期 | 无 | `str` |
| `time_now_int()` | 获取当前时间戳整形 | 无 | `int` |
| `time_future_date()` | 未来的随机年月日 | 无 | `str` |
| `time_future_datetime()` | 未来的随机年月日时分秒 | 无 | `str` |
| `time_today_date_00()` | 获取今日00:00:00时间 | 无 | `str` |
| `time_today_date_59()` | 获取今日23:59:59时间 | 无 | `str` |
| `time_after_week()` | 获取一周后12点整的时间 | 无 | `str` |
| `time_after_month()` | 获取30天后的12点整时间 | 无 | `str` |
| `time_random_year_str()` | 随机年份字符串 | 无 | `str` |
| `time_random_month_str()` | 随机月份字符串（01-12） | 无 | `str` |
| `time_random_day_str()` | 随机日字符串（01-31） | 无 | `str` |
| `time_random_hour_str()` | 随机小时字符串（00-23） | 无 | `str` |
| `time_random_minute_str()` | 随机分钟字符串（00-59） | 无 | `str` |
| `time_random_second_str()` | 随机秒字符串（00-59） | 无 | `str` |
| `time_random_ym()` | 随机年月字符串（YYYY-MM） | 无 | `str` |
| `time_random_ymd_str()` | 随机年月日字符串（YYYY-MM-DD） | 无 | `str` |
| `time_random_ymdhm_str()` | 随机年月日时分字符串（YYYY-MM-DD HH:MM） | 无 | `str` |
| `time_random_ymdhms_str()` | 随机年月日时分秒字符串（YYYY-MM-DD HH:MM:SS） | 无 | `str` |
| `time_random_hm_str()` | 随机时分字符串（HH:MM） | 无 | `str` |
| `time_random_hms_str()` | 随机时分秒字符串（HH:MM:SS） | 无 | `str` |
| `time_random_timestamp_s()` | 随机时间戳（秒） | 无 | `int` |
| `time_random_timestamp_ms()` | 随机时间戳（毫秒） | 无 | `int` |
| `time_random_timestamp_us()` | 随机时间戳（微秒） | 无 | `int` |
| `time_random_iso8601()` | 随机ISO8601时间字符串 | 无 | `str` |
| `time_random_rfc3339()` | 随机RFC3339时间字符串 | 无 | `str` |
| `time_random_future_ymd()` | 未来随机年月日字符串 | 无 | `str` |
| `time_random_past_ymd()` | 过去随机年月日字符串 | 无 | `str` |
| `time_random_future_ymdhms()` | 未来随机年月日时分秒字符串 | 无 | `str` |
| `time_random_past_ymdhms()` | 过去随机年月日时分秒字符串 | 无 | `str` |
| `time_random_weekday()` | 随机周几（中文） | 无 | `str` |
| `time_random_weekday_num()` | 随机周几（数字1-7） | 无 | `int` |
| `time_random_quarter()` | 随机季度（Q1-Q4） | 无 | `str` |
| `time_random_12h()` | 随机12小时制时间字符串（hh:MM:SS AM/PM） | 无 | `str` |
| `time_random_time_range_str()` | 随机时间区间字符串（如'2023-01-01~2023-01-31'） | 无 | `str` |
| `time_random_utc_offset()` | 随机UTC偏移字符串（如+08:00） | 无 | `str` |
| `time_random_rfc2822()` | 随机RFC2822时间字符串 | 无 | `str` |
| `time_random_rfc850()` | 随机RFC850时间字符串 | 无 | `str` |
| `time_random_rfc1123()` | 随机RFC1123时间字符串 | 无 | `str` |
| `time_random_rfc1036()` | 随机RFC1036时间字符串 | 无 | `str` |
| `time_random_rfc822()` | 随机RFC822时间字符串 | 无 | `str` |
| `time_random_rfc3339nano()` | 随机RFC3339纳秒时间字符串 | 无 | `str` |
| `time_cron_time(time_parts)` | 秒级cron表达式 | `time_parts`: 时间部分列表 | `str` |
| `time_next_minute_cron(minutes=1)` | 按周重复的cron表达式 | `minutes`: 分钟偏移(默认1) | `str` |
| `time_random_cron()` | 随机cron表达式（分 时 日 月 周） | 无 | `str` |

---

## JsonTool - JSON 处理工具

用于处理 JSON 数据的转换和提取。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `load(json_str)` | 将json字符串转换成Python对象 | `json_str`: JSON字符串 | `dict \| list` |
| `dump(obj, indent=None)` | 将Python对象转换成json字符串 | `obj`: Python对象, `indent`: 缩进 | `str` |
| `loads(json_list_str)` | 将json数组字符串转换成Python列表 | `json_list_str`: JSON数组字符串 | `list \| dict` |
| `dumps(obj_list, indent=None)` | 将Python列表转换成json数组字符串 | `obj_list`: Python列表, `indent`: 缩进 | `str` |
| `flatten(json_obj, sep='_', prefix='')` | 将嵌套的json对象展开成扁平的字典 | `json_obj`: JSON对象, `sep`: 分隔符, `prefix`: 前缀 | `dict` |
| `get_json_path_value(obj, expr, index=0)` | 在dict中根据jsonpath取出值 | `obj`: 字典, `expr`: jsonpath表达式, `index`: 索引 | `any` |

---

## CacheTool - 内存缓存工具

用于内存中的数据缓存管理。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `get_cache(key)` | 得到缓存key的value | `key`: 缓存键 | `any` |
| `set_cache(key, value)` | 设置一个内容到缓存 | `key`: 缓存键, `value`: 缓存值 | `None` |
| `delete_cache(key)` | 删除一个缓存 | `key`: 缓存键 | `None` |
| `clear_cache()` | 清理所有缓存 | 无 | `None` |
| `has_cache(key)` | 判断缓存是否存在 | `key`: 缓存键 | `bool` |
| `get_all()` | 获取全部的缓存数据 | 无 | `dict` |
| `set_sql_cache(key, value)` | 根据sql进行缓存数据 | `key`: SQL键, `value`: 值字典 | `None` |
| `set_list_cache(key, value)` | 设置list缓存 | `key`: 键列表, `value`: 值列表 | `None` |
| `set_dict_cache(data)` | 设置dict缓存 | `data`: 数据字典 | `None` |

---

## CodingTool - 编码工具

用于数据的编码和解码操作。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `base64_encode(data)` | 编码字符串 | `data`: 要编码的字符串 | `str` |
| `response_decoding(data)` | 解码字符串 | `data`: 要解码的数据 | `str` |

---

## EncryptionTool - 加密工具

用于数据的 MD5 加密操作。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `md5_32_small(string)` | MD5_32位小写加密 | `string`: 要加密的字符串 | `str` |
| `md5_32_large(string)` | MD5_32位大写加密 | `string`: 要加密的字符串 | `str` |
| `md5_16_small(string)` | MD5_16位小写加密 | `string`: 要加密的字符串 | `str` |
| `md5_16_large(string)` | MD5_16位大写加密 | `string`: 要加密的字符串 | `str` |

---

## SqlCache - SQL 缓存工具

用于基于 SQLite 的数据持久化缓存。

| 方法名 | 描述 | 参数 | 返回值类型 |
|--------|------|------|------------|
| `get_sql_cache(key)` | 获取缓存中指定键的值 | `key`: 缓存键 | `Union[str, list, dict, int, float, bool, tuple, None]` |
| `set_sql_cache(key, value, value_type)` | 设置缓存键的值 | `key`: 缓存键, `value`: 缓存值, `value_type`: 值类型枚举 | `None` |
| `delete_sql_cache(key)` | 删除缓存中指定键的值 | `key`: 缓存键 | `None` |
| `contains_sql_cache(key)` | 检查缓存中是否包含指定键 | `key`: 缓存键 | `bool` |
| `clear_sql_cache()` | 清空缓存中的所有键值对 | 无 | `None` |
| `get_sql_all()` | 获取所有缓存数据 | 无 | `dict` |

---

## 使用示例

```python
from mangotools.data_processor import DataProcessor

# 创建数据处理器实例
processor = DataProcessor()

# 获取随机手机号码
phone = processor.character_phone()
print(phone)  # 输出: 13812345678

# 获取随机姓名
name = processor.character_male_name()
print(name)  # 输出: 张三

# 获取当前时间
current_time = processor.time_now_ymdhms()
print(current_time)  # 输出: 2024-01-15 10:30:45

# 获取随机数字
random_num = processor.randint(1, 100)
print(random_num)  # 输出: 42

# MD5加密
encrypted = processor.md5_32_small("hello")
print(encrypted)  # 输出: 5d41402abc4b2a76b9719d911017c592
```

---

## 导出列表

以下是可以通过 `DataProcessor` 类直接调用的所有方法：

**RandomCharacterInfoData 方法：**
- `character_phone`, `character_id_number`, `character_female_name`, `character_male_name`, `character_simple_profile`, `character_profile`, `character_email`, `character_bank_card`, `character_address`, `character_job`, `character_company`, `str_company_job`, `str_job`, `str_university`, `str_intl_phone`, `str_license_plate`, `str_postcode`, `str_company_email`

**RandomNumberData 方法：**
- `randint`, `number_time_5`, `number_random_0_9`, `number_random_10_99`, `number_random_100_999`, `number_random_0_5000`, `number_random_float`, `number_random_two_float`, `number_random_1000_two_float`, `number_random_even`, `number_random_odd`, `number_random_positive_float`, `number_random_negative_float`, `number_random_scientific`, `number_random_percent`, `number_random_money`, `number_random_mobile`, `number_random_code4`, `number_random_code6`, `number_random_prime`, `number_random_negative_even`, `number_random_negative_odd`, `number_random_decimal`, `number_positive_infinity`, `number_negative_infinity`, `number_nan`, `number_random_exponential`, `number_random_fraction_str`, `number_random_roman`, `number_random_bin_str`, `number_random_oct_str`, `number_random_hex_str`

**RandomStringData 方法：**
- `str_uuid`, `str_random_string`, `str_city`, `str_country`, `str_province`, `str_pystr`, `str_word`, `str_text`, `str_lowercase`, `str_uppercase`, `str_special_chars`, `str_url`, `str_ipv4`, `str_mac_address`, `str_ipv6`, `str_date`, `str_time`, `str_datetime`, `str_url_path`, `str_hex_color`, `str_uuid_no_dash`, `str_en_sentence`, `str_en_paragraph`, `str_cn_address`, `str_domain`, `str_filename`, `str_file_path`, `str_image_url`, `str_latlng`, `str_json`, `str_base64`, `str_emoji`, `str_color_name`, `str_markdown`, `str_sql`, `str_url_params`, `str_card_number_split`, `str_isbn`, `str_vin`, `str_wechat_id`, `str_qq`, `str_short_url`, `str_special_mix`, `str_whitespace`, `str_url_encoded`, `str_json_nested`, `str_html_escape`, `str_with_emoji`

**RandomTimeData 方法：**
- `time_now_ymdhms`, `time_random_utcnow`, `time_now_ymd`, `time_now_hms`, `time_now_ymd_h`, `time_now_ymd_hm`, `time_stamp`, `time_next_minute`, `time_now_year`, `time_now_month`, `time_now_day`, `time_now_hour`, `time_now_minute`, `time_now_second`, `time_random_year`, `time_random_month`, `time_random_date`, `time_now_int`, `time_future_date`, `time_future_datetime`, `time_today_date_00`, `time_today_date_59`, `time_after_week`, `time_after_month`, `time_random_year_str`, `time_random_month_str`, `time_random_day_str`, `time_random_hour_str`, `time_random_minute_str`, `time_random_second_str`, `time_random_ym`, `time_random_ymd_str`, `time_random_ymdhm_str`, `time_random_ymdhms_str`, `time_random_hm_str`, `time_random_hms_str`, `time_random_timestamp_s`, `time_random_timestamp_ms`, `time_random_timestamp_us`, `time_random_iso8601`, `time_random_rfc3339`, `time_random_future_ymd`, `time_random_past_ymd`, `time_random_future_ymdhms`, `time_random_past_ymdhms`, `time_random_weekday`, `time_random_weekday_num`, `time_random_quarter`, `time_random_12h`, `time_random_time_range_str`, `time_random_utc_offset`, `time_random_rfc2822`, `time_random_rfc850`, `time_random_rfc1123`, `time_random_rfc1036`, `time_random_rfc822`, `time_random_rfc3339nano`, `time_cron_time`, `time_next_minute_cron`, `time_random_cron`

**JsonTool 方法：**
- `load`, `dump`, `loads`, `dumps`, `flatten`, `get_json_path_value`

**CacheTool 方法：**
- `get_cache`, `set_cache`, `delete_cache`, `clear_cache`, `has_cache`, `get_all`, `set_sql_cache`, `set_list_cache`, `set_dict_cache`

**CodingTool 方法：**
- `base64_encode`, `response_decoding`

**EncryptionTool 方法：**
- `md5_32_small`, `md5_32_large`, `md5_16_small`, `md5_16_large`

---

*文档生成时间：2026-04-21*
